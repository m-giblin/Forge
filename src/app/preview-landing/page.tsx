import LandingPage from "@/components/marketing/LandingPage";

// Auth-free preview of the marketing landing page.
// Remove this route before going to production if you don't want it public.
export default function PreviewLanding() {
  return <LandingPage />;
}
